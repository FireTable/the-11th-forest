/**
 * vitest.setup.ts
 * --------------------------------------------------------------------------
 * Currently empty. Per-file overrides (e.g. `@vitest-environment jsdom`)
 * handle browser-specific cases.
 */

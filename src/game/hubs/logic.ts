/**
 * src/game/hubs/logic.ts
 * --------------------------------------------------------------------------
 * Placeholder for HUD-module pure helpers. Reserved for future use when
 * HUD event handlers need pure decision functions extracted from the
 * hub classes for testing — e.g.:
 *   - canPickup(currentLoadout, newSpec): boolean
 *   - whichSlotsHighlighted(activeIndex, hoveredIndex): boolean
 *   - calculateAmmoDisplay(current, max): string
 *
 * Currently empty — hub-specific pure helpers live in `base-hub.ts`,
 * each hub class owns its own rendering. Add helpers here as features grow.
 */
export {};
